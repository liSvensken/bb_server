export interface UserPostResponse {
  // id: number;
  // role: string;
  // name: string;
  // email: string;
  // services?: any; // todo Obj в виде number[]
  // city?: number;
  //
  // photo?: string;
  // phone?: string;
  // gender?: string;
  // birthday?: string;
  // infoYourself?: string;
  // infoServices?: any[]; // ссылка на инфоСервисез (id, crater, service, text)
  // photosGallery?: any[]; // ссылка на ФотозГалереи (id, crater, service, photo)
}
